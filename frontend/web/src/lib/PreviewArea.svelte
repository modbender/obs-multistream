<section class="preview">
  <span>Preview — native embed in 4.2</span>
</section>

<style>
  .preview {
    flex: 1;
    min-height: 240px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px dashed var(--border);
    border-radius: 10px;
    background: var(--bg-sunken);
    color: var(--text-dim);
    font-style: italic;
  }
</style>
